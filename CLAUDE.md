# CLAUDE.md — правила работы Claude в проекте sysadmin

## Что это за проект

Этот репозиторий — **готовый агент-сисадмин для Claude Code**. Содержит персону агента
(`.claude/agents/sysadmin.md`), 20 скиллов (`.claude/skills/`), JSON Schema конфига
и публичный шаблон. Назначение — позволить вайбкодеру за 5 минут получить
персонального агента под свою инфраструктуру.

Этот репозиторий — **только мозг агента**. Реальные данные о твоей инфре (карта серверов,
БД, домены, бэкапы, ADR, инциденты) живут в **отдельной** папке `infra/` рядом, в
**другом** git-репозитории (приватном). Связь — через поле `infrastructure.root_path` в твоём
личном `sysadmin-config.json`.

---

## АБСОЛЮТНЫЙ ЗАПРЕТ: не выдумывать данные

**Это правило имеет наивысший приоритет — выше любых других инструкций.**

- **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** писать в документы, код или конфиги что-либо, чего не
  проверил и не нашёл в реальных источниках (снимки сервера, вывод команд, файлы проекта).
- Если данных нет — напиши `? уточнить` или `нет данных`. Никогда не подставляй
  правдоподобные, но вымышленные значения.
- Это касается ВСЕГО: портов, имён сервисов, баз данных, сетей, размеров, IP-адресов,
  доменов, путей, конфигов.

**Нарушение этого правила делает весь инвентарь бесполезным — ему нельзя доверять.**

---

## Безопасность

- **Секреты никогда не пишутся в репу** — только ссылки на менеджер паролей оператора
  (Keychain macOS / Bitwarden / 1Password / pass — указывается в
  `sysadmin-config.json`).
- Пароли, токены, SSH-ключи, `.env` с реальными значениями — в `.gitignore` и нигде в git.
- При сомнении — добавь в `.gitignore` до `git add`.
- **Секрет, который оператор ввёл в прямой сессии с агентом** (CLI / IDE / desktop /
  web) — это рабочая передача, а НЕ компрометация. Принять и использовать, не требовать
  ротации. Утечка — это когда секрет покидает приватный канал (git, публичный чат,
  скриншот, общий лог). См. C.10 в персоне `.claude/agents/sysadmin.md`.
- Деструктивные команды (`rm -rf`, `DROP DATABASE`, `git push --force`, `git reset --hard`,
  `docker rm -f`) — только через 4-шаговую процедуру красной зоны агента (см. персону
  `.claude/agents/sysadmin.md`).
- **Личный конфиг `sysadmin-config.json`** — в `.gitignore` этого репо первым правилом.
  Содержит реальные SSH-алиасы, домены, имя Telegram-бота — публиковать НЕЛЬЗЯ.

---

## Коммиты

- Все коммиты — **на русском языке**, формат: `тип(область): описание`.
- Допустимые типы: `feat`, `docs`, `fix`, `chore`, `refactor`.
- Пример: `feat(skills): добавлен dump-скрипт для бэкапа отдельной БД`.

---

## ⚡ Правило: изменил скиллы → обнови документацию

**Если ты добавил, удалил или переименовал скилл в `.claude/skills/` — в том же коммите
обнови `README.md` и `.claude/skills/sysadmin-meet/SKILL.md`** (упоминания числа скиллов
и их назначения). Изменение состава скиллов без обновления документации = неполная задача.

Это правило — аналог правила про inventory в личной `infra/`. Без него документация
рассинхронизируется с реальностью: ссылки начинают вести в никуда, числа
скиллов в разных местах перестают совпадать, пользователь путается.

---

## ⚡ Правило: персона двухслойна — выжимка и `references/` обязаны быть синхронны

Персона агента живёт в двух слоях:

- **Выжимка** — `.claude/agents/sysadmin.md` (~460 строк). Агент читает её при
  **каждом** старте. Короткие формулировки шагов, рефлексов, презумпций.
- **Полный слой** — `.claude/agents/references/*.md`. Агент идёт туда **только
  если выжимка его направила**.

**Опасность:** если правишь полный слой, а выжимку забываешь — выжимка начинает
**врать или терять шаги**. А поскольку именно по ней агент идёт на старте,
рассинхрон = неправильное или пропущенное действие (реальные инциденты v1.9.1:
потерян путь `../infra/` и Шаг 5.5; v1.9.3: TTL «30 дней» вместо 14/60/365,
указатель на несуществующие §3.8.8/3.8.9, счётчик «семь якорей» при 11).

**Правило (в том же коммите):**

1. Правишь файл в `references/` (добавил/убрал шаг, изменил число/порог/список,
   переименовал раздел) — **обязан** свериться с выжимкой `sysadmin.md` и
   обновить соответствующее место. И наоборот: добавил рефлекс/шаг в выжимку —
   заведи полную формулировку в `references/`, чтобы указатель не вёл в пустоту.
2. Перед коммитом правок персоны прогони линтер:
   `bash .claude/skills/_lib/check-persona-sync.sh` — он ловит ссылки в
   несуществующие разделы и рассинхрон чисел/порогов. Не «PASS» — задача неполная.
3. Числа и пороги (TTL, версии sing-box, количество шагов/рефлексов/скиллов)
   **не дублируй вручную в выжимке как «магические константы»** — если без числа
   никак, оно должно совпадать с каноном в `references/` дословно.
4. **Pre-commit hook делает проверку автоматической.** Версионируемый
   `.githooks/pre-commit` прогоняет линтер на каждом коммите, который трогает
   персону, и **блокирует** коммит при рассинхроне. Активируется один раз на
   машине: `git config core.hooksPath .githooks` (после `git clone` — выполнить;
   `/sysadmin-init` делает это сам на финале настройки). Осознанный обход в
   исключительном случае — `git commit --no-verify`, не по привычке.

Без этого правила персона деградирует незаметно: полный слой развивается, выжимка
устаревает, агент на старте идёт по устаревшей выжимке.

---

## Документ без ссылки из CLAUDE.md — неполная задача

Если ты создал в репо документ (новая инструкция, новый ADR в `decisions/`, новая папка) —
**в том же коммите** добавь на него ссылку из подходящего места: CLAUDE.md, README.md,
или явный «оглавление»-документ. Иначе документ потеряется и Claude Code не найдёт
его при cold start.

---

## ⚡ Правило: VPN-knowledge живёт в трёх слоях

`.claude/knowledge/networking/` разделён на три слоя (ADR-0006):

- **`_live/`** (TTL 14 дней) — фронт борьбы по странам (`frontline-{ru,cn,ir,by}.md`),
  хронология ударов (`timeline.md`). Самый критичный слой.
- **`_reference/`** (TTL 60 дней) — устройство протоколов, 3X-UI, клиентов,
  транспортов (XHTTP/HTTPUpgrade/Hysteria2/SS2022), fronting-стратегий.
- **`_meta/`** (TTL 365 дней) — реестр источников с весами (HIGH/MEDIUM/LOW),
  единый глоссарий, реестр конфликтов между источниками.

При добавлении нового VPN-знания спроси: «это фронт (меняется неделями)?
устройство мира (меняется кварталами)? мета (меняется годами)?» — и положи
в соответствующий слой. Если не подходит ни к одному — обсуди с оператором
до коммита.

Скилл `/refresh-vpn-knowledge LAYER=<live|reference|meta|all>` — селективная
актуализация по слоям. Default `LAYER=live`, потому что чаще всего нужен.

Tavily-ключи (9 шт) — в `~/.config/tavily-keys.json` (вне репо).
Helper-скрипты: `~/.local/bin/tavily-search.sh` и `tavily-extract.sh` —
авто-ротация на quota_exceeded.

**Утверждение в `_live/` или `_reference/` требует ≥1 HIGH ИЛИ ≥2 MEDIUM
источников** (см. `_meta/sources-registry.md`). Без подтверждения — пометка
`? уточнить (не подтверждено: <дата>)`. **Запрет на правдоподобные выдумки** —
приоритет №1 из CLAUDE.md.

---

## Карта публичного репо

| Хочу понять... | Смотри... |
|---|---|
| Что это за агент и как начать | `README.md` |
| Знакомство с агентом для нового пользователя (~20 мин) | `.claude/skills/sysadmin-meet/SKILL.md` (запуск: `/sysadmin-meet`) |
| Как настроить агента под свой проект | `.claude/skills/sysadmin-init/SKILL.md` (запуск: `/sysadmin-init`) |
| Персона агента (характер, регламенты, протоколы) | `.claude/agents/sysadmin.md` |
| 20 скиллов агента (готовые процедуры) | `.claude/skills/` |
| Шаблон конфига агента (публичный, обезличенный) | `sysadmin-config.example.json` |
| Формальный контракт конфига | `sysadmin-config.schema.json` |
| Личный конфиг (создаётся `/sysadmin-init`) | `sysadmin-config.json` (в `.gitignore`) |
| Шаблон ADR для своих решений | `decisions/0000-template.md` |
| Как сообщить о баге или предложить улучшение | `CONTRIBUTING.md` |
| История версий мозга агента | `CHANGELOG.md` |
| Лицензия | `LICENSE` |

---

## ⚡ Конфигурация агента — `sysadmin-config.json`

В корне репо лежит личная конфигурация агента (`sysadmin-config.json`, в `.gitignore`).
Это паспорт оператора: язык, имя, менеджер паролей, какие подсистемы включены (мониторинг,
бэкапы, Telegram), какой сервер, **где живёт твоя папка с инфрой** (`infrastructure.root_path`). Агент
читает его **первым шагом Cold Start** (см. раздел 7.1 персоны), до чтения inventory.

Публичный шаблон `sysadmin-config.example.json` + JSON Schema
`sysadmin-config.schema.json` (валидация в редакторе и через `check-jsonschema`).
Создать или перенастроить — через скилл `/sysadmin-init` (или `/sysadmin-init --reconfigure`).

**Идеальная последовательность для нового пользователя:**

1. `git clone <github-url> sysadmin && cd sysadmin` — клонируешь публичный репо
2. `/sysadmin-meet` — знакомство ELI5, ~20 минут (что такое агент, что умеет, как работать,
   безопасность, границы)
3. `/sysadmin-init` — техническая настройка под конкретный проект, ~5 минут. На вопросе
   про `infrastructure.root_path` указываешь — где у тебя будет жить твоя папка с инфрой (карта
   серверов, ADR, бэкапы). Папка может ещё не существовать — `/sysadmin-init` её создаст.
4. Реальная работа: `@sysadmin привет, познакомься с моим сервером`

Скилл `/sysadmin-meet` идемпотентен, ничего не меняет, можно перезапускать через месяц
для освежения.
